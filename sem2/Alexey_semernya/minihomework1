#include <iostream>
#include <vector>
#include <string>
int main() {
	auto filter{ [](std::vector<std::string> data, char sim) {int c = 0; int s = size(data); for (int i = 0; i < s; i++) { if (data[i][0] == sim) { c++; } } return c; } };
}
