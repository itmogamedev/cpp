#pragma once
#include <vector>

float Average(std::vector <int>);
void Mediana(std::vector <int>);
float Mean_square1(std::vector <int> mass);
float Mean_square2(std::vector <int> mass);
float Dispersion(std::vector <int> mass);